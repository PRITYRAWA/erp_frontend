import React from 'react'
import { Wrapper } from '../../components'
import System from '../skelton/System'
import ListView from './ListView'

function DashboardView(props) {













    return (
        <>



            {/* <System
                para={props?.para}
                menuToggle={props.menuToggle} list_info={props.list_info} form_mode={props.form_mode}
                getPreviousMenu={props.getPreviousMenu} /> */}


            {/* <ListView para={props.para} title={props.list_info.label}
                // getPreviousMenu={getPreviousMenu}
                menuToggle={props.menuToggle}
                // searchControl={searchControl}
                // changeMode={changeMode}
                // loadFormData={loadFormData}
                // defaultHeaders={props.list_info.columns}
                // headers={props.list_info.columns}
                // headers={header}
                data_source={props.list_info.data_source}
                list_info={props.list_info}
            // importData={importData}
            // rows={listRows}
            /> */}





        </>

    )
}

export default DashboardView